import Login from '@/components/AdminDashboard/Login';

export const metadata = {
    title: 'Admin Login | Aadesh Lawate',
    description: 'Secure access to the portfolio administration dashboard.',
};

export default function LoginPage() {
    return <Login />;
}
